'use strict';

var config = require('./config.json');

require('./utils').fetchAll(config, function handleResult(err) {
    setTimeout(function defer() {
        process.exit(err ? 1 : 0);
    }, 100);

    if (err) {
        throw err;
    }
    console.log('Done fetching header/footer');
});
