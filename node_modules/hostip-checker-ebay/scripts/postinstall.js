'use strict';
var util = require('./util');

util.createLocalCacheCopy(function(err, result) {
    if (err) {
        console.error('Could not load IP List from Service, Using already existing copy', err.message);
    }

    process.exit(0);
});
