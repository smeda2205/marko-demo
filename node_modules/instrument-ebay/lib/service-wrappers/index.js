'use strict';

var wrappers = require('instrument-inc/wrappers');

['https', 'cookies',  'tracking', 'outbound-context', 'oauth', 'soap', 'ep', 'cache'].forEach(function (name) {
    wrappers.register(name, require('./'+name));
}, {});
