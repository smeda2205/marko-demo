'use strict';

class CounterBucket {

    constructor (windowStart) {
        this.windowStart = windowStart;
        this.value = 0;
    }

    get() {
        return this.value;
    }

    increment() {
        this.value++;
    }
}

module.exports = CounterBucket;
