'use strict';

var component = require('./index');

module.exports = function(input, out) {
    /* The service-use tag relies on the call stack. Marko V3 has a different stack, so 
    adding this extra intermediate call fixes the issue, without changing the original code */

    component(input, out);
};
