'use strict';

var objutil = require('objutil'),
    Activity = require('./activity'),
    MessageClass = require('../constants').MessageClass;


function Event() {
    Event.super_.apply(this, arguments);
    this.messageClass = MessageClass.EVENT;
}

objutil.extend(Event, Activity);


exports = module.exports = Event;
